# 关爱独居老人志愿服务系统


一个基于 Go + Gin + GORM 开发的后端服务系统。  
用于模拟「独居老人定时问候 + 超时自动通知最近志愿者」的应急关怀平台。

---

## 📌 项目背景

针对独居老人无人照看的情况：

- 系统每天向老人发送问候信息
- 若老人超过 2 小时未回复
- 自动匹配最近志愿者并生成告警记录

目标：

> 构建一个具备 **自动调度 + 距离匹配 + 定时任务** 的后端系统原型。

---

## 🧱 技术栈

- Golang
- Gin Web Framework
- GORM ORM
- MySQL
- 定时任务（goroutine + ticker）
- Haversine 距离算法

---

## 🏗 项目结构
elder-care-volunteer/
├── config/ # 数据库连接
├── models/ # 数据表模型
├── handlers/ # API接口
├── services/ # 业务逻辑层
├── routes/ # 路由注册
├── tasks/ # 定时任务
└── main.go


## ⚙️ 核心功能

### 👴 老人注册

POST /api/elders/register
注册信息：
- 手机号
- 姓名
- 地址
- 经纬度
- 监护人电话


### 🧑‍🤝‍🧑 志愿者注册

包含：

- 手机号
- 姓名
- 地址
- 经纬度
- 是否可用

---

### ⏱ 定时未回复检测

后台任务：
tasks/check_no_reply.go

流程：

1. 每分钟扫描老人列表
2. 判断是否超过 2 小时未回复
3. 自动触发志愿者匹配

---

### 📍 最近志愿者匹配

使用：

老人坐标 vs 志愿者坐标

最近且 available = 1
生成：

alert_record

字段：

- elder_id
- volunteer_id
- distance_km
- status (pending)
- created_at
- notified_at

并包含：
防重复告警机制

## 🔄 系统流程

老人注册
↓
定时任务扫描
↓
超过2小时未回复
↓
匹配最近志愿者
↓
创建 alert_record
↓
模拟通知

## 📡 API 列表

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/elders/register | POST | 老人注册 |
| /api/volunteers/register | POST | 志愿者注册 |
| /api/volunteers/available | GET | 可用志愿者 |
| /api/elders/:id/no-reply | POST | 模拟未回复 |
| /api/elders/:id/reply | POST | 老人回复 |

